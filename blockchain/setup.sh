#!/usr/bin/env bash
# Obsolete fixed-topology interface.
set -euo pipefail

echo "[ERROR] blockchain/setup.sh was removed with the fixed topology; use deploy.py chain-init --inventory INVENTORY" >&2
exit 2
