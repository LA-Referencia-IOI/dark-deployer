# dARK deployer runtime for CPython 3.14.
# Keep only direct dependencies here; pip resolves compatible transitive wheels
# for the running architecture instead of pinning Python-3.12-era internals.
py-solc-x>=2.0.5,<3
cryptography>=50,<51
python-dotenv>=1.2,<2
jsonschema>=4.26,<5
PyYAML>=6.0.3,<7
requests>=2.34,<3
web3>=7.16,<8
